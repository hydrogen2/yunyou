# Role: Publisher / Producer

Owns `tour.json` (assembled from scenes, valid against `studio/schema/tour.schema.json`) and the linear-cut plan:
title, thumbnail brief, description, chapters/timestamps, platform list, drip schedule. Writes `review/publish-record.md`.
